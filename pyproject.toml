[project]
name = "sourdough-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-core>=0.3.0",
    "langchain-google-genai>=2.0.0",
    "langgraph>=0.2.0",
    "langgraph-checkpoint-sqlite>=2.0.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "python-telegram-bot>=21.0",
    "requests",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
